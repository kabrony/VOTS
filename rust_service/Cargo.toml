[package]
name = "rust_service"
version = "0.1.0"
edition = "2021"

[dependencies]
hyper = { version = "0.14", features = ["full"] }
tokio = { version = "1.32", features = ["rt-multi-thread", "macros"] }
